# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

#### Интерфейс IProduct
Описывает структуру данных отдельного товара.

Поля интерфейса:
`id: string` - уникальный идентификатор товара.
`description: string` - детальное описание товара.
`image: string` - URL-адрес картинки товара.
`title: string` - название товара.
`category: string` - категория, к которой относится товар.
`price: number | null` - цена товара.

#### Интерфейс IBuyer
Описывает структуру данных покупателя, необходимых для оформления заказа.

Поля интерфейса:
`payment: TPayment | null` - способ оплаты.
`email: string` - электронная почта покупателя.
`phone: string` - телефон покупателя.
`address: string` - адрес доставки.

#### Интерфейс IOrder
Описывает структуру данных заказа, подготавливаемого для отправки на сервер. Расширяет интерфейс `IBuyer`.

Поля интерфейса:
`total: number` - общая сумма заказа.
`items: string[]` - массив идентификаторов купленных товаров.

#### Интерфейс IProductListResponse
Описывает структуру ответа от сервера на запрос списка товаров.

Поля интерфейса:
`total: number` - общее количество доступных товаров на сервере.
`items: IProduct[]` - массив объектов товаров.

#### Интерфейс IOrderResult
Описывает структуру ответа от сервера после успешного оформления заказа.

Поля интерфейса:
`id: string` - уникальный идентификатор созданного заказа.
`total: number` - сумма заказа, списанная сервером.

### Модели данных

#### Класс CatalogModel
Отвечает за хранение массива всех товаров (каталога) и товара, который пользователь выбрал для подробного просмотра в модальном окне.

Конструктор класса:
`constructor(events: IEvents)` - принимает экземпляр брокера событий.

Поля класса:
`items: IProduct[]` - массив всех доступных товаров.
`preview: IProduct | null` - хранит товар, выбранный для подробного отображения.

Методы класса:
`setItems(items: IProduct[]): void` - сохраняет массив товаров в модель и вызывает событие обновления каталога.
`getItems(): IProduct[]` - возвращает массив всех товаров.
`getProduct(id: string): IProduct | undefined` - возвращает один товар по его id.
`setPreviewProduct(item: IProduct): void` - сохраняет товар для подробного отображения и генерирует событие открытия модального окна.
`getPreviewProduct(): IProduct | null` - возвращает товар для подробного отображения.

#### Класс CartModel
Отвечает за хранение товаров, выбранных покупателем для покупки (корзина). 

Конструктор класса:
`constructor(events: IEvents)` - принимает экземпляр брокера событий.

Поля класса:
`items: IProduct[]` - массив товаров, добавленных в корзину.

Методы класса:
`getProducts(): IProduct[]` - возвращает массив товаров, которые находятся в корзине.
`addProduct(item: IProduct): void` - добавляет товар, переданный в параметре, в массив корзины.
`removeProduct(id: string): void` - удаляет товар по id из массива корзины.
`clear(): void` - очищает корзину.
`getTotalPrice(): number` - возвращает сумму стоимости всех товаров в корзине.
`getTotalCount(): number` - возвращает количество товаров в корзине.
`contains(id: string): boolean` - проверяет наличие товара в корзине по его id.

#### Класс BuyerModel
Отвечает за хранение данных покупателя, которые тот должен указать при оформлении заказа. Выполняет валидацию введенных данных.

Конструктор класса:
`constructor(events: IEvents)` - принимает экземпляр брокера событий.

Поля класса:
`payment: TPayment | null` - вид оплаты.
`email: string` - почта покупателя.
`phone: string` - телефон покупателя.
`address: string` - адрес доставки.

Методы класса:
`setField(field: keyof IBuyer, value: string): void` - сохраняет полученное значение в соответствующее поле модели.
`getData(): IBuyer` - возвращает объект со всеми данными покупателя.
`clear(): void` - очищает данные покупателя.
`validate(): BuyerErrors` - проверяет поля на наличие ошибок и возвращает объект с текстами ошибок.

### Слой коммуникации

#### Класс LarekApi
Отвечает за взаимодействие с сервером. В классе реализована композиция: он оборачивает в себя базовый класс через интерфейс `IApi`.

Конструктор класса:
`constructor(baseApi: IApi)` - принимает базовый URL файлов и готовый экземпляр базового класса Api через интерфейс `IApi`.

Поля класса:
`baseApi: IApi` - экземпляр класса для выполнения HTTP-запросов.

Методы класса:
`getProductList(): Promise<IProductListResponse>` - выполняет GET-запрос на эндпоинт `/product/`. Возвращает промис с объектом, содержащим список товаров и их общее количество.
`orderProducts(order: IOrder): Promise<IOrderResult>` - выполняет POST-запрос на эндпоинт `/order/` для оформления заказа. Передает объект заказа и возвращает промис с ответом сервера.

### Слой представления

#### Класс Header
Отвечает за отображение шапки сайта, где находится логотип и кнопка корзины со счетчиком товаров.

Конструктор класса:  
`constructor(container: HTMLElement, events: EventEmitter)` - принимает DOM-элемент и экземпляр брокера событий.

Поля класса:  
`protected counterElement: HTMLElement` - элемент счетчика товаров на кнопке корзины.  
`protected basketButton: HTMLButtonElement` - кнопка корзины.

Методы и сеттеры класса:  
`set counter(value: number)` - обновляет значение счетчика товаров в шапке.

#### Класс Gallery
Отвечает за отображение каталога товаров на главной странице.

Конструктор класса:  
`constructor(container: HTMLElement, events: EventEmitter)` - принимает DOM-элемент и экземпляр брокера событий.

Поля класса:
`protected catalogElement: HTMLElement` - корневой элемент галереи, куда вставляются карточки. 

Методы и сеттеры класса:  
`set catalog(items: HTMLElement[])` - принимает массив сгенерированных карточек каталога и заменяет ими текущее содержимое контейнера галереи.

#### Класс Modal
Компонент модального окна, является самостоятельным элементом.

Конструктор класса:  
`constructor(container: HTMLElement, events: EventEmitter)` - принимает DOM-элемент и экземпляр брокера событий.

Поля класса:  
`protected contentElement: HTMLElement` - контейнер внутри модального окна для вставки содержимого.  
`protected closeButton: HTMLButtonElement` - кнопка закрытия модального окна.

Методы и сеттеры класса:
`set content(value: HTMLElement)` - принимает сгенерированный DOM-элемент контента и вставляет его в модальное окно.
`open(): void` - показывает модальное окно.  
`close(): void` - скрывает модальное окно и очищает контент.

#### Класс Success
Компонент, выводящий информацию об успешном завершении оформления заказа.

Конструктор класса:  
`constructor(container: HTMLElement, events: EventEmitter)` - принимает DOM-элемент и экземпляр брокера событий.

Поля класса:  
`protected closeButton: HTMLButtonElement` - кнопка закрытия в окне успешного заказа.  
`protected descriptionElement: HTMLElement` - элемент, содержащий описание суммы списания.

Методы и сеттеры класса:  
`set total(value: number)` - обновляет текст с информацией о списанной сумме.

#### Класс BaseForm
Базовый класс для компонентов, представляющих формы.

Конструктор класса:  
`constructor(container: HTMLFormElement, events: EventEmitter)` - принимает DOM-элемент и экземпляр брокера событий.

Поля класса:  
`protected submitButton: HTMLButtonElement` - кнопка отправки формы.  
`protected errorsElement: HTMLElement` - блок для вывода ошибок валидации.  

Методы и сеттеры класса:  
`set errors(value: string)` - принимает текст ошибки и выводит его в соответствующий блок.  
`set validate(value: boolean)` - принимает состояние валидности формы.

#### Класс OrderForm
Управляет формой первого шага оформления заказа - выбор способа оплаты и ввода адреса. Наследуется от класса `BaseForm`.

Конструктор класса:  
`constructor(container: HTMLFormElement, events: EventEmitter)` - принимает DOM-элемент и экземпляр брокера событий.

Поля класса:
`protected buttons: HTMLButtonElement[]` - коллекция кнопок выбора способа оплаты.

Методы и сеттеры класса:
`set payment(value: string)` - устанавливает выбранный способ оплаты.
`set address(value: string)` - устанавливает значение в поле ввода адреса.

#### Класс ContactsForm
Управляет формой второго шага оформления заказа - ввод электронной почты и номера телефона. Наследуется от класса `BaseForm`.

Конструктор класса:  
`constructor(container: HTMLFormElement, events: EventEmitter)` - принимает DOM-элемент и экземпляр брокера событий.

Методы и сеттеры класса:
`set phone(value: string)` - устанавливает значение в поле ввода телефона.
`set email(value: string)` - устанавливает значение в поле ввода адреса электронной почты.

#### Класс CardBase
Базовый класс для всех вариаций карточек товара.

Конструктор класса:  
`constructor(container: HTMLElement)` - принимает DOM-элемент.

Поля класса:   
`protected titleElement: HTMLElement` - элемент заголовка карточки.  
`protected priceElement: HTMLElement` - элемент цены карточки.

Методы и сеттеры класса:  
`set title(value: string)` - обновляет содержимое заголовка карточки.  
`set price(value: number | null)` - обновляет значение цены карточки.

#### Класс CardCatalog
Представляет карточку товара при отображении в каталоге. Наследуется от класса `CardBase`.

Конструктор класса:  
`constructor(container: HTMLElement, actions?: ICardActions)` - принимает DOM-элемент и объект с коллбэками для обработки кликов.

Поля класса:  
`protected categoryElement: HTMLElement` - элемент категории.
`protected imageElement: HTMLImageElement` - элемент изображения.

Методы и сеттеры класса:  
`set category(value: string): void` - устанавливает текст категории товара.
`set image(src: string): void` - устанавливает путь к изображению товара.
`set imageAlt(value: string): void` - устанавливает альтернативный текст изображения в атрибут.

#### Класс CardPreview
Представляет детализированную карточку товара для отображения в модальном окне. Наследуется от класса `CardBase`.

Конструктор класса:  
`constructor(container: HTMLElement, actions?: ICardActions)` - принимает DOM-элемент и объект с коллбэками для обработки кликов.

Поля класса:  
`protected imageElement: HTMLImageElement` - элемент изображения товара.
`protected categoryElement: HTMLElement` - элемент категории.
`protected descriptionElement: HTMLElement` - элемент описания товара.
`protected actionButton: HTMLButtonElement` - кнопка действия.

Методы и сеттеры класса:  
`set image(src: string): void` - устанавливает путь к изображению товара.
`set imageAlt(value: string): void` - устанавливает альтернативный текст изображения.
`set category(value: string): void` - устанавливает текст категории и модифицирует класс при необходимости.
`set description(value: string): void` - устанавливает текст описания товара.
`set buttonTitle(value: string): void` - устанавливает текст на кнопке действия.
`set buttonIsDisabled(value: boolean): void` - переключает доступность (активность) кнопки действия.

#### Класс CardBasket
Представляет компактную карточку товара для отображения в списке корзины. Наследуется от класса `CardBase`.

Конструктор класса:  
`constructor(container: HTMLElement, actions?: ICardActions)` - принимает DOM-элемент и объект с коллбэками для обработки кликов.

Поля класса:  
`protected indexElement: HTMLElement` - элемент с порядковым номером.  
`protected deleteButton: HTMLButtonElement` - кнопка удаления элемента корзины.

Методы и сеттеры класса:  
`set index(value: number): void` - устанавливает порядковый номер в `indexElement`.

#### Класс Basket
Отвечает за отображение корзины товаров.

Конструктор класса:  
`constructor(container: HTMLElement, events: EventEmitter)` - принимает DOM-элемент и экземпляр брокера событий.

Поля класса:  
`protected listElement: HTMLUListElement` - контейнер для списка карточек.  
`protected priceElement: HTMLElement` - элемент отображения итоговой стоимости заказа.  
`protected orderButton: HTMLButtonElement` - кнопка для оформления заказа.

Методы и сеттеры класса:
`set items(value: HTMLElement[])` - принимает массив выбранных товаров и отображает их.
`set total(value: number)` - обновляет значение общей стоимости.

### Описание событий

#### Описание событий слоя представлений
`card:select` - выбор карточки для просмотра.
`preview:action` - нажатие кнопки покупки товара.
`card:remove` - нажатие кнопки удаления товара из корзины.
`basket:open` - нажатие кнопки открытия корзины.
`basket:checkout` - нажатие кнопки оформления заказа.
`forms:change` - изменение данных в формах.
`order:submit` - нажатие кнопки перехода ко второй форме оформления заказа.
`contacts:submit` - нажатие кнопки оплаты/завершения оформления заказа.
`success:close` - нажатие кнопки «За новыми покупками» в окне успешного заказа.

#### Описание событий слоя моделей
`catalog:change` - изменение каталога товаров.
`preview:change` - изменение выбранного для просмотра товара.
`basket:change` - изменение содержимого корзины.
`buyer:change` - изменение данных покупателя.

### Презентер
В приложении не выделен отдельный класс Презентера. Согласно архитектуре MVP, слой управления (Презентер) реализован в основном скрипте приложения `main.ts`. 

Код выполняет инициализацию всех необходимых экземпляров классов (Моделей, Представлений и API), использует глобальный брокер событий `EventEmitter` для их связывания и управляет бизнес-логикой путем установки слушателей.